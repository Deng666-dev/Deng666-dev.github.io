---
title: one
date: 2026-01-02 17:12:02
tags: 开发
categories: [开发笔记]
sticky: 0
---

```php
    你的代码内容
```

```bash
    # 安装 Samba 服务
    yum install samba samba-client -y
    systemctl start smb
```

```yaml
    inject:123
    head:345
        - <style>...</style>
```

```ini
    [global]
    workgroup = WORKGROUP
    security = user
```

```python
    import base64
    print("Vodka is Pro")
```

```ini /etc/samba/smb.conf
    [Share]
    comment = My Share
    path = /srv/samba
    writable = yes
```

```diff
    # 修改 _config.butterfly.yml
    inject:
    head:
-       - <style>错误的代码</style>
+       - <style>正确的代码</style>
```

请按 `Win + R` 打开运行框，输入 `cmd`，然后去 `/etc/hosts` 查看。